const defaultOptions: TypeRequiredOptions = {
  min: 0,
  max: 10,
  from: -Infinity,
  to: Infinity,
  step: 1,
  isInterval: false,
  isVertical: false,
  isLabel: true,
  isScale: true,
};

export default defaultOptions;
